# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules/
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.sql
*.md
/Postman/*

# 포트폴리오 산출물(HTML) — 개인 결과물, git 미추적 (프론트 소스 index.html 은 추적)
docs/**/*.html

# vendored 외부 repo / 외부 심링크 (Haru 코드 아님)
awesome-codex-subagents/
.codex/agents

# 환경변수 (DB 자격증명 등 — 절대 커밋 금지). .env.example 은 추적함
.env
**/.env
!.env.example
!**/.env.example

# TLS 인증서/개인키 — 절대 커밋 금지 (public 노출 방지)
**/nginx/ssl/
*.pem
*.key
*.pfx
*.jks
*.keystore