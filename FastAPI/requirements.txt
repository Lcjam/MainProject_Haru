fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0
pydantic
aiohttp  # HTTP 클라이언트/서버를 위한 비동기 라이브러리
# AI 모델: llama.cpp(gguf) 기반. Apple Silicon 에서는 Metal 백엔드 자동 사용.
# (이전 transformers+torch 방식은 대용량/CPU float16 이슈로 gguf 로 전환)
llama-cpp-python
